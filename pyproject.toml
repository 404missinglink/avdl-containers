[project]
name = "avdl-containers"
version = "0.1.0"
description = "Accelerated Vision Deep Learning containers (PyTorch, TorchCodec, FFmpeg on AWS DLC)"
dependencies = [
    "torch",
    "torchcodec",
]

[tool.uv]
dev-dependencies = []
